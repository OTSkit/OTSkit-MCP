startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    description: No configuration required. No API keys needed.
  commandFunction: |-
    (config) => ({
      command: "npx",
      args: ["-y", "@otskit/mcp", "serve"],
      env: {}
    })
